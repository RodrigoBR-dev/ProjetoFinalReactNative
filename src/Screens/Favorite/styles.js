import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  containerHome: {
    flex: 1,
  },

  container: {
    alignItems: "center",
  },

  info: {
    paddingTop: 10,
    alignItems: "center",
  },
});

export default styles;
